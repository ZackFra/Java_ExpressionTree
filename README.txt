To instantiate a new expression tree enter ExpressionTree tree = new ExpressionTree(expr) 
where expr is a string containing an expression.
To evaluate the value of an expression tree, call tree.eval().
To get the expression in postfix notation call tree.toString(). 
